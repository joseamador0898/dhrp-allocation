# DHRP-8 benchmark dependencies (Python 3.10-3.12 recommended)
#
# Tested on Google Colab Pro (Python 3.10) and Ubuntu 22.04.

# Core scientific stack
numpy>=1.23,<3
pandas>=1.5,<3        # pandas-datareader 0.10 is incompatible with pandas>=3
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3
torch>=2.0
cvxpy>=1.4
pydantic>=2

# Visualization
matplotlib>=3.6
seaborn>=0.12

# Data sources (Yahoo Finance via yfinance, Fama-French via pandas-datareader,
# FRED macro via fredapi)
yfinance>=0.2
requests>=2.28
pandas-datareader>=0.10
python-dotenv>=1.0
fredapi>=0.5

# Notebook execution (for `bash scripts/reproduce_all.sh`)
jupyter>=1.0
nbconvert>=7.0

# LLM features (FinBERT via transformers, FinLang via sentence-transformers)
transformers>=4.46
tokenizers>=0.20
huggingface-hub>=0.26
sentence-transformers>=3.0
